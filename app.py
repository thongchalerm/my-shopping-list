import streamlit as st
import pandas as pd
import os

# ตั้งชื่อไฟล์สำหรับเก็บข้อมูล
DATA_FILE = "my_shopping_list.csv"

# ฟังก์ชันสำหรับโหลดข้อมูล
def load_data():
    if os.path.exists(DATA_FILE):
        return pd.read_csv(DATA_FILE)
    else:
        return pd.DataFrame(columns=["ชื่อสินค้า", "แพลตฟอร์ม", "หมวดหมู่", "ลิงก์สินค้า"])

# ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="My Shopping Shortcuts", layout="wide")
st.title("📦 เครื่องมือบันทึกรายการซื้อซ้ำ")

# โหลดข้อมูลปัจจุบัน
df = load_data()

# --- ส่วนที่ 1: เพิ่มสินค้าใหม่ ---
with st.expander("➕ เพิ่มรายการสินค้าใหม่"):
    col1, col2 = st.columns(2)
    with col1:
        item_name = st.text_input("ชื่อสินค้า")
        platform = st.selectbox("แพลตฟอร์ม", ["Lazada", "Shopee", "อื่นๆ"])
    with col2:
        category = st.text_input("หมวดหมู่ (เช่น อุปกรณ์ร้าน, ของใช้ในบ้าน)")
        link = st.text_input("วางลิงก์สินค้าที่นี่")

    if st.button("บันทึกข้อมูล"):
        if item_name and link:
            new_data = pd.DataFrame([[item_name, platform, category, link]], 
                                    columns=["ชื่อสินค้า", "แพลตฟอร์ม", "หมวดหมู่", "ลิงก์สินค้า"])
            df = pd.concat([df, new_data], ignore_index=True)
            df.to_csv(DATA_FILE, index=False)
            st.success(f"บันทึก {item_name} เรียบร้อยแล้ว!")
            st.rerun()
        else:
            st.error("กรุณากรอกชื่อสินค้าและวางลิงก์ด้วยครับ")

# --- ส่วนที่ 2: แสดงรายการและค้นหา ---
st.divider()
search_query = st.text_input("🔍 ค้นหาสินค้า...")

if not df.empty:
    # กรองข้อมูลตามคำค้นหา
    filtered_df = df[df['ชื่อสินค้า'].str.contains(search_query, case=False) | 
                     df['หมวดหมู่'].str.contains(search_query, case=False)]

    # แสดงตารางพร้อมลิงก์ที่คลิกได้
    st.dataframe(
        filtered_df,
        column_config={
            "ลิงก์สินค้า": st.column_config.LinkColumn("คลิกเพื่อไปที่ร้านค้า")
        },
        use_container_width=True,
        hide_index=True
    )
    
    # ปุ่มสำหรับล้างข้อมูลทั้งหมด (เผื่อต้องการเริ่มใหม่)
    if st.button("ลบรายการทั้งหมด"):
        if os.path.exists(DATA_FILE):
            os.remove(DATA_FILE)
            st.rerun()
else:
    st.info("ยังไม่มีข้อมูลสินค้า เริ่มเพิ่มข้อมูลที่ส่วนด้านบนได้เลยครับ")